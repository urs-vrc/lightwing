// Code generated by the Encore v1.58.4 client generator. DO NOT EDIT.

// Disable eslint, jshint, and jslint for this file.
/* eslint-disable */
/* jshint ignore:start */
/*jslint-disable*/

/**
 * BaseURL is the base URL for calling the Encore application's API.
 */
export type BaseURL = string

export const Local: BaseURL = "http://localhost:4000"

/**
 * Environment returns a BaseURL for calling the cloud environment with the given name.
 */
export function Environment(name: string): BaseURL {
    return `https://${name}-poow4.encr.app`
}

/**
 * PreviewEnv returns a BaseURL for calling the preview environment with the given PR number.
 */
export function PreviewEnv(pr: number | string): BaseURL {
    return Environment(`pr${pr}`)
}

const BROWSER = typeof globalThis === "object" && ("window" in globalThis);

/**
 * Client is an API client for the poow4 Encore application.
 */
export default class Client {
    public readonly auth: auth.ServiceClient
    public readonly eventmanager: eventmanager.ServiceClient
    public readonly teammanager: teammanager.ServiceClient
    private readonly options: ClientOptions
    private readonly target: string


    /**
     * Creates a Client for calling the public and authenticated APIs of your Encore application.
     *
     * @param target  The target which the client should be configured to use. See Local and Environment for options.
     * @param options Options for the client
     */
    constructor(target: BaseURL, options?: ClientOptions) {
        this.target = target
        this.options = options ?? {}
        const base = new BaseClient(this.target, this.options)
        this.auth = new auth.ServiceClient(base)
        this.eventmanager = new eventmanager.ServiceClient(base)
        this.teammanager = new teammanager.ServiceClient(base)
    }

    /**
     * Creates a new Encore client with the given client options set.
     *
     * @param options Client options to set. They are merged with existing options.
     **/
    public with(options: ClientOptions): Client {
        return new Client(this.target, {
            ...this.options,
            ...options,
        })
    }
}

/**
 * ClientOptions allows you to override any default behaviour within the generated Encore client.
 */
export interface ClientOptions {
    /**
     * By default the client will use the inbuilt fetch function for making the API requests.
     * however you can override it with your own implementation here if you want to run custom
     * code on each API request made or response received.
     */
    fetcher?: Fetcher

    /** Default RequestInit to be used for the client */
    requestInit?: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }
}

export namespace auth {
    /**
     * GetSessionResponse mirrors the session shape returned by get-session and
     * stored by the frontend in localStorage.
     * 
     * Frontend stores: lightwing:session:token
     * Sends:   Authorization: Bearer ***
     */
    export interface GetSessionResponse {
        session: SessionInfo
        user: UserProfile
    }

    /**
     * GetUserBySlugParams carries the slug query param.
     */
    export interface GetUserBySlugParams {
        Slug: string
    }

    /**
     * ListUsersRequest carries the auth header plus search/pagination query params.
     * 
     * Mirrors ts-legacy/auth/users.ts listUsers (GET /api/users)
     */
    export interface ListUsersRequest {
        Authorization: string
        Search: string
        Limit: number
        Offset: number
    }

    /**
     * ListUsersResponse is the paginated response for listUsers.
     */
    export interface ListUsersResponse {
        users: UserProfile[]
        total: number
    }

    /**
     * SessionInfo is the session portion of GetSessionResponse.
     */
    export interface SessionInfo {
        token: string
        expiresAt: string
    }

    /**
     * SetUserSiteRoleRequest carries the target id, auth header, and new role.
     * (The id travels in the body rather than a :id path param because this
     * Encore version only accepts scalar params alongside path params.)
     * 
     * Mirrors ts-legacy/auth/users.ts setUserSiteRole
     * (PUT /api/users/:id/site-role)
     */
    export interface SetUserSiteRoleRequest {
        id: string
        Authorization: string
        siteRole: string
    }

    /**
     * SignInSocialParams provides the OAuth redirect URL for the callback.
     */
    export interface SignInSocialParams {
        /**
         * CallbackURL, if provided, indicates where the frontend expects to be
         * redirected after a successful sign-in callback.
         */
        CallbackURL: string
    }

    /**
     * SignInSocialResponse directs the client to the Discord authorization URL.
     */
    export interface SignInSocialResponse {
        redirectUrl: string
    }

    /**
     * TeamAffiliation represents a user's membership in an organization.
     * 
     * Mirrors ts-legacy/auth/users.ts TeamAffiliation
     */
    export interface TeamAffiliation {
        organizationId: string
        name: string
        slug: string
        role: string
    }

    /**
     * UpdateUserRequest carries the target id, auth header, and editable fields.
     * (The id travels in the body rather than a :id path param because this
     * Encore version only accepts scalar params alongside path params.)
     * 
     * Mirrors ts-legacy/auth/users.ts updateUserProfile (PATCH /api/users/:id)
     */
    export interface UpdateUserRequest {
        id: string
        Authorization: string
        name: string
        slug: string
        image: string
        biography: string
        careerOverview: string
        vrchatUsername: string
    }

    /**
     * UserProfile is the public representation of a user, as returned by
     * get-session and the user management endpoints.
     * 
     * Mirrors ts-legacy/auth/users.ts UserProfile. Nullable columns are
     * pointers so they serialize as null when unset, and Name prefers the
     * VRChat display name exactly like TS toProfile does.
     */
    export interface UserProfile {
        id: string
        name: string
        slug: string
        email: string
        image: string
        biography: string
        careerOverview: string
        vrchatUsername: string
        classTier: string
        siteRole: string
        teams: TeamAffiliation[]
        createdAt: string
        updatedAt: string
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.Callback = this.Callback.bind(this)
            this.GetSession = this.GetSession.bind(this)
            this.GetUser = this.GetUser.bind(this)
            this.GetUserBySlug = this.GetUserBySlug.bind(this)
            this.ListUsers = this.ListUsers.bind(this)
            this.SetUserSiteRole = this.SetUserSiteRole.bind(this)
            this.SignInSocial = this.SignInSocial.bind(this)
            this.SignOut = this.SignOut.bind(this)
            this.UpdateUser = this.UpdateUser.bind(this)
        }

        /**
         * Callback handles the Discord OAuth redirect. It exchanges the code for a token,
         * fetches the user profile from Discord, upserts the user/account/session in the
         * database, then redirects back to the frontend with the session token as a
         * URL fragment (so the SPA can extract it).
         */
        public async Callback(method: "GET", body?: RequestInit["body"], options?: CallParameters): Promise<globalThis.Response> {
            return this.baseClient.callAPI(method, `/api/auth/callback/discord`, body, options)
        }

        /**
         * GetSession returns the session and user profile for the authenticated caller.
         */
        public async GetSession(): Promise<GetSessionResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/auth.GetSession`)
            return await resp.json() as GetSessionResponse
        }

        /**
         * GetUser returns a participant's public profile.
         * 
         * Mirrors ts-legacy/auth/users.ts getUserProfile (GET /api/users/:id)
         */
        public async GetUser(id: string): Promise<UserProfile> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/users/${encodeURIComponent(id)}`)
            return await resp.json() as UserProfile
        }

        /**
         * GetUserBySlug returns a participant's public profile by slug.
         * 
         * Mirrors ts-legacy/auth/users.ts getUserProfileBySlug. It lives at
         * /api/users-by-slug (rather than nested under /api/users/) because static
         * sub-paths route-conflict with /api/users/:id under Encore's router.
         */
        public async GetUserBySlug(params: GetUserBySlugParams): Promise<UserProfile> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                slug: params.Slug,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/users-by-slug`, undefined, {query})
            return await resp.json() as UserProfile
        }

        public async ListUsers(params: ListUsersRequest): Promise<ListUsersResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            const query = makeRecord<string, string | string[]>({
                limit:  String(params.Limit),
                offset: String(params.Offset),
                search: params.Search,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/users`, undefined, {headers, query})
            return await resp.json() as ListUsersResponse
        }

        public async SetUserSiteRole(params: SetUserSiteRoleRequest): Promise<UserProfile> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                id:       params.id,
                siteRole: params.siteRole,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/api/users/site-role`, JSON.stringify(body), {headers})
            return await resp.json() as UserProfile
        }

        /**
         * SignInSocial returns a redirect URL to start the Discord OAuth flow.
         */
        public async SignInSocial(params: SignInSocialParams): Promise<SignInSocialResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                callbackUrl: params.CallbackURL,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/auth/sign-in/social`, undefined, {query})
            return await resp.json() as SignInSocialResponse
        }

        /**
         * SignOut deletes the caller's session.
         */
        public async SignOut(): Promise<void> {
            await this.baseClient.callTypedAPI("POST", `/auth.SignOut`)
        }

        public async UpdateUser(params: UpdateUserRequest): Promise<UserProfile> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                biography:      params.biography,
                careerOverview: params.careerOverview,
                id:             params.id,
                image:          params.image,
                name:           params.name,
                slug:           params.slug,
                vrchatUsername: params.vrchatUsername,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/api/users`, JSON.stringify(body), {headers})
            return await resp.json() as UserProfile
        }
    }
}

export namespace eventmanager {
    /**
     * AddEventAdminRequest carries the event/user ids plus the auth header.
     * The ids travel in the body (rather than :id path params) because this
     * Encore version only accepts scalar params alongside path params.
     * 
     * Mirrors ts-legacy/eventmanager/events.ts addEventAdmin (POST /api/events/:id/admins).
     */
    export interface AddEventAdminRequest {
        eventId: string
        userId: string
        Authorization: string
    }

    /**
     * AddEventAdminResponse reports success.
     */
    export interface AddEventAdminResponse {
        success: boolean
    }

    /**
     * AddEventMemberRequest carries the event/user ids plus the auth header.
     * 
     * Mirrors ts-legacy/eventmanager/event-members.ts AddMemberParams
     * (POST /api/events/:id/members).
     */
    export interface AddEventMemberRequest {
        eventId: string
        userId: string
        Authorization: string
    }

    /**
     * AddEventScheduleRequest carries the schedule payload plus the auth header.
     * 
     * Mirrors ts-legacy/eventmanager/event-schedules.ts AddScheduleParams
     * (POST /api/events/:id/schedules).
     */
    export interface AddEventScheduleRequest {
        eventId: string
        title: string
        startsAt: string
        endsAt: string
        location: string
        Authorization: string
    }

    /**
     * AssignRaceEventRequest mirrors AssignRaceResultParams. Result fields are
     * explicit (rather than an embedded struct) so they appear in the generated
     * API client schema.
     */
    export interface AssignRaceResultRequest {
        eventId: string
        raceId: string
        userId: string
        Authorization: string
        position: number
        points: number
        gateNumber: number
        finishTime: string
        margin: string
        passingOrder: string
        final3F: string
        resultStatus: string
        clearPosition: boolean
        clearStatus: boolean
    }

    /**
     * BulkResultsRequest mirrors Replace/MergeRaceResultsParams.
     */
    export interface BulkResultsRequest {
        eventId: string
        raceId: string
        Authorization: string
        results: RaceResultInput[]
    }

    /**
     * ClassTierInfo describes one skill tier.
     */
    export interface ClassTierInfo {
        tier: string
        label: string
        rank: number
    }

    /**
     * ClassTiersResponse wraps the ordered tier list.
     */
    export interface ClassTiersResponse {
        tiers: ClassTierInfo[]
    }

    /**
     * CreateDatasetRequest mirrors CreateDatasetParams.
     */
    export interface CreateDatasetRequest {
        eventId: string
        Authorization: string
        source: string
        rows: number
        status: string
    }

    /**
     * CreateEventRequest carries the create payload plus the auth header.
     * 
     * Mirrors ts-legacy/eventmanager/events.ts CreateEventParams (POST /api/events).
     */
    export interface CreateEventRequest {
        Authorization: string
        name: string
        description: string
        ownerType: string
        organizationId: string
        ownerUserId: string
        tag: string
        scoringType: number
        scoringRulesMode: string
        customScoringTables: JSONValue
        classRestriction: string
        granularParticipation: boolean
        scheduledAt: string
        participantLimit: OptInt
        maxConcurrentRaceParticipations: OptInt
    }

    /**
     * CreateRaceEventRequest mirrors CreateRaceEventParams (POST /api/events/:eventId/races).
     */
    export interface CreateRaceEventRequest {
        eventId: string
        Authorization: string
        name: string
        sequence: number
        distanceMeters: number
        trackType: string
        location: string
        scoringType: number
        grade: string
        classRestriction: string
        startsAt: string
        endsAt: string
        participantLimit: OptInt
    }

    /**
     * DatasetView mirrors the TS DatasetView.
     */
    export interface DatasetView {
        id: string
        eventId: string
        source: string
        rows: number
        status: string
        importedAt: string
        createdAt: string
        updatedAt: string
    }

    /**
     * DatasetsQuery scopes dataset listing by event.
     */
    export interface DatasetsQuery {
        EventID: string
    }

    /**
     * DatasetsResponse wraps the dataset list.
     */
    export interface DatasetsResponse {
        datasets: DatasetView[]
    }

    /**
     * DeleteEventRequest carries the event id plus the auth header (DELETE
     * decodes the struct from query params).
     */
    export interface DeleteEventRequest {
        ID: string
        Authorization: string
        Permanent: boolean
    }

    /**
     * DeleteEventResponse reports deletion.
     */
    export interface DeleteEventResponse {
        deleted: boolean
    }

    /**
     * DeleteRaceEventRequest mirrors DeleteRaceEventParams.
     */
    export interface DeleteRaceEventRequest {
        eventId: string
        raceId: string
        Authorization: string
    }

    /**
     * DeleteRaceEventResponse reports deletion.
     */
    export interface DeleteRaceEventResponse {
        deleted: boolean
    }

    /**
     * DeleteRaceResultRequest mirrors DeleteRaceResultParams.
     */
    export interface DeleteRaceResultRequest {
        eventId: string
        raceId: string
        userId: string
        Authorization: string
    }

    /**
     * DeleteRaceResultResponse reports deletion.
     */
    export interface DeleteRaceResultResponse {
        deleted: boolean
    }

    /**
     * EligibleEvent is one event with enterable races.
     */
    export interface EligibleEvent {
        id: string
        name: string
        organizationId: string
        classRestriction: string
        eligibleRaces: EligibleRace[]
    }

    /**
     * EligibleEventsQuery fetches events a participant may enter.
     */
    export interface EligibleEventsQuery {
        UserID: string
    }

    /**
     * EligibleEventsResponse wraps the eligible event list.
     */
    export interface EligibleEventsResponse {
        events: EligibleEvent[]
    }

    /**
     * EligibleRace is one race the participant may enter.
     */
    export interface EligibleRace {
        id: string
        name: string
        sequence: number
        classRestriction: string
    }

    /**
     * EventAdminView is one event administrator.
     */
    export interface EventAdminView {
        userId: string
        name: string
    }

    /**
     * EventDetail is the full event payload.
     * 
     * Mirrors ts-legacy/eventmanager/events.ts EventDetail.
     */
    export interface EventDetail {
        id: string
        name: string
        description: string
        ownerType: string
        organizationId: string
        ownerUserId: string
        status: string
        tag: string
        deletedAt: string
        scoringType: number
        scoringTypeLabel: string
        scoringRulesMode: string
        customScoringTables: JSONValue
        classRestriction: string
        granularParticipation: boolean
        signupsLocked: boolean
        scheduledAt: string
        participantLimit: number
        maxConcurrentRaceParticipations: number
        raceEvents: RaceEventView[]
        members: EventMemberView[]
        schedules: EventScheduleView[]
        pointsOverview: PointsEntryView[]
        ladderOverview: LadderEntryView[]
        createdAt: string
        updatedAt: string
    }

    /**
     * EventListItem is the summary row returned by list endpoints.
     */
    export interface EventListItem {
        id: string
        name: string
        description: string
        ownerType: string
        organizationId: string
        ownerUserId: string
        status: string
        tag: string
        deletedAt: string
        scoringType: number
        scoringTypeLabel: string
        classRestriction: string
        granularParticipation: boolean
        signupsLocked: boolean
        scheduledAt: string
        participantLimit: number
        maxConcurrentRaceParticipations: number
        raceCount: number
        memberCount: number
        createdAt: string
        updatedAt: string
    }

    /**
     * EventMemberView is a single event participant.
     */
    export interface EventMemberView {
        userId: string
        name: string
        classTier: string
    }

    /**
     * EventScheduleView is a single schedule slot.
     */
    export interface EventScheduleView {
        id: string
        title: string
        startsAt: string
        endsAt: string
        location: string
    }

    /**
     * JoinEventRequest carries the event id plus the auth header.
     * 
     * Mirrors ts-legacy/eventmanager/event-members.ts JoinEventParams
     * (POST /api/events/:id/join).
     */
    export interface JoinEventRequest {
        eventId: string
        Authorization: string
    }

    /**
     * LadderEntryView is one row of the ladder overview.
     */
    export interface LadderEntryView {
        userId: string
        name: string
        elo: number
        wins: number
        losses: number
        rank: number
    }

    /**
     * LadderMatchRequest mirrors LadderMatchParams (POST /api/events/:id/ladder/matches).
     */
    export interface LadderMatchRequest {
        id: string
        Authorization: string
        winnerId: string
        loserId: string
    }

    /**
     * LeaveEventRequest carries the event id plus the auth header (DELETE
     * decodes the struct from query params).
     * 
     * Mirrors ts-legacy/eventmanager/event-members.ts LeaveEventParams
     * (DELETE /api/events/:id/join).
     */
    export interface LeaveEventRequest {
        EventID: string
        Authorization: string
    }

    /**
     * ListEventAdminsResponse carries the admin list.
     */
    export interface ListEventAdminsResponse {
        admins: EventAdminView[]
    }

    /**
     * ListEventsQuery carries optional filters plus pagination.
     * 
     * Mirrors ts-legacy/eventmanager/events.ts ListEventsParams (GET /api/events).
     */
    export interface ListEventsQuery {
        OrganizationID: string
        ClassRestriction: string
        Status: string
        Tag: string
        IncludeDeleted: boolean
        Limit: number
        Offset: number
    }

    /**
     * ListEventsResponse carries the page plus the total count.
     */
    export interface ListEventsResponse {
        events: EventListItem[]
        total: number
    }

    /**
     * ListPublicEventsQuery carries pagination for the public listing.
     */
    export interface ListPublicEventsQuery {
        Limit: number
        Offset: number
    }

    /**
     * ListRaceEventsQuery lists races of an event ordered by sequence.
     */
    export interface ListRaceEventsQuery {
        EventID: string
    }

    /**
     * OptInt is a tri-state integer for create/update payloads: unset (field
     * omitted), explicit null (clear the limit), or a positive value.
     * It mirrors the TS `number | null | undefined` limit fields.
     * 
     * Mirrors ts-legacy/eventmanager/participation-limits.ts parseOptionalPositiveInt.
     */
    export interface OptInt {
        Set: boolean
        Value: number
    }

    /**
     * OptString is a tri-state string: unset (omit), explicit null (clear), or a value.
     * It mirrors the TS `string | null | undefined` update fields.
     */
    export interface OptString {
        Set: boolean
        Value: string
    }

    /**
     * PointsEntryView is one row of the points overview.
     */
    export interface PointsEntryView {
        userId: string
        name: string
        points: number
    }

    /**
     * RaceEventDetail is the full race view returned by race endpoints, mirroring
     * ts-legacy/eventmanager/raceevents.ts RaceEventDetail. Members reuse the
     * shared RaceEventMemberView from events.go.
     */
    export interface RaceEventDetail {
        id: string
        eventId: string
        name: string
        sequence: number
        distanceMeters: number
        trackType: string
        location: string
        scoringType: number
        grade: string
        classRestriction: string
        startsAt: string
        endsAt: string
        participantLimit: number
        createdAt: string
        updatedAt: string
        members: RaceEventMemberView[]
    }

    /**
     * RaceEventMemberView is a single race participant.
     */
    export interface RaceEventMemberView {
        userId: string
        name: string
        classTier: string
    }

    /**
     * RaceEventQuery fetches a single race.
     */
    export interface RaceEventQuery {
        EventID: string
        RaceID: string
    }

    /**
     * RaceEventView is a race belonging to an event.
     */
    export interface RaceEventView {
        id: string
        name: string
        sequence: number
        distanceMeters: number
        trackType: string
        location: string
        scoringType: number
        grade: string
        classRestriction: string
        startsAt: string
        endsAt: string
        participantLimit: number
        members: RaceEventMemberView[]
    }

    /**
     * RaceJoinRequest carries event/race ids plus the caller's auth header.
     */
    export interface RaceJoinRequest {
        eventId: string
        raceId: string
        Authorization: string
    }

    /**
     * RaceMemberRequest carries event/race/user ids for member mutations.
     */
    export interface RaceMemberRequest {
        eventId: string
        raceId: string
        userId: string
        Authorization: string
    }

    /**
     * RaceMembersQuery lists participants of a race.
     */
    export interface RaceMembersQuery {
        EventID: string
        RaceID: string
    }

    /**
     * RaceMembersResponse wraps the member list.
     */
    export interface RaceMembersResponse {
        members: RaceEventMemberView[]
    }

    /**
     * RaceResultInput mirrors the TS RaceResultInput: nil means omitted (leave
     * unchanged on update), while non-nil values (including explicit zero/empty)
     * overwrite. Explicit-null clearing is exposed via Clear* flags.
     */
    export interface RaceResultInput {
        userId: string
        position: number
        points: number
        gateNumber: number
        finishTime: string
        margin: string
        passingOrder: string
        final3F: string
        resultStatus: string
        clearPosition: boolean
        clearStatus: boolean
    }

    /**
     * RaceResultView is the API view of one participant's result on a race.
     */
    export interface RaceResultView {
        id: string
        raceEventId: string
        userId: string
        position: number
        points: number
        gateNumber: number
        finishTime: string
        margin: string
        passingOrder: string
        final3F: string
        resultStatus: string
        createdAt: string
        updatedAt: string
    }

    /**
     * RaceResultsQuery lists results of a race.
     */
    export interface RaceResultsQuery {
        EventID: string
        RaceID: string
    }

    /**
     * RaceResultsResponse wraps the ordered result list.
     */
    export interface RaceResultsResponse {
        results: RaceResultView[]
    }

    /**
     * RecomputePointsRequest mirrors RecomputePointsParams.
     */
    export interface RecomputePointsRequest {
        id: string
        Authorization: string
    }

    /**
     * RecomputePointsResponse reports success.
     */
    export interface RecomputePointsResponse {
        success: boolean
    }

    /**
     * RemoveEventAdminRequest carries the event/user ids plus the auth header
     * (DELETE decodes the struct from query params).
     * 
     * Mirrors ts-legacy/eventmanager/events.ts removeEventAdmin.
     */
    export interface RemoveEventAdminRequest {
        EventID: string
        UserID: string
        Authorization: string
    }

    /**
     * RemoveEventMemberRequest carries the event/user ids plus the auth header
     * (DELETE decodes the struct from query params).
     * 
     * Mirrors ts-legacy/eventmanager/event-members.ts RemoveMemberParams
     * (DELETE /api/events/:id/members/:userId).
     */
    export interface RemoveEventMemberRequest {
        EventID: string
        UserID: string
        Authorization: string
    }

    /**
     * ReorderRaceEventsRequest mirrors ReorderRaceEventsParams.
     */
    export interface ReorderRaceEventsRequest {
        eventId: string
        Authorization: string
        orderedRaceIds: string[]
    }

    /**
     * ReorderRaceEventsResponse returns the reordered race list.
     */
    export interface ReorderRaceEventsResponse {
        races: RaceEventDetail[]
    }

    /**
     * RestoreEventRequest carries the event id plus the auth header.
     */
    export interface RestoreEventRequest {
        id: string
        Authorization: string
    }

    /**
     * SetEventPointsRequest mirrors SetPointsParams (PUT /api/events/:id/points/:userId).
     */
    export interface SetEventPointsRequest {
        id: string
        userId: string
        Authorization: string
        points: number
    }

    /**
     * SetEventSignupsLockedRequest toggles the signup lock plus the auth header.
     * 
     * Mirrors ts-legacy/eventmanager/event-members.ts SetSignupsLockedParams
     * (PUT /api/events/:id/signups-lock).
     */
    export interface SetEventSignupsLockedRequest {
        eventId: string
        locked: boolean
        Authorization: string
    }

    /**
     * SetEventStatusRequest mirrors SetStatusParams (PUT /api/events/:id/status).
     * Endorsing an event with OFFICIAL tag is reserved for site administrators.
     */
    export interface SetEventStatusRequest {
        id: string
        Authorization: string
        status: string
        tag: string
    }

    /**
     * SetUserClassRequest mirrors SetUserClassParams (PUT /api/users/:userId/class).
     * Within an organization it is gated by the event-update grant; site admins
     * may set a tier globally.
     */
    export interface SetUserClassRequest {
        userId: string
        Authorization: string
        organizationId: string
        classTier: string
    }

    /**
     * SetUserClassResponse echoes the assignment.
     */
    export interface SetUserClassResponse {
        userId: string
        classTier: string
    }

    /**
     * UpdateDatasetStatusRequest mirrors UpdateDatasetStatusParams.
     */
    export interface UpdateDatasetStatusRequest {
        eventId: string
        datasetId: string
        Authorization: string
        status: string
    }

    /**
     * UpdateEventRequest carries the editable fields plus the auth header. The
     * event id travels in the body (rather than a :id path param) because this
     * Encore version only accepts scalar params alongside path params.
     * 
     * Mirrors ts-legacy/eventmanager/event-updates.ts UpdateEventParams
     * (PATCH /api/events/:id). Scoring type is immutable once set.
     */
    export interface UpdateEventRequest {
        id: string
        Authorization: string
        name: string
        description: OptString
        tag: string
        scoringRulesMode: string
        customScoringTables: JSONValue
        classRestriction: OptString
        scheduledAt: OptString
        participantLimit: OptInt
        maxConcurrentRaceParticipations: OptInt
    }

    /**
     * UpdateRaceEventRequest mirrors UpdateRaceEventParams. Pointer fields
     * distinguish omitted (nil, leave unchanged) from explicit null (clear).
     */
    export interface UpdateRaceEventRequest {
        eventId: string
        raceId: string
        Authorization: string
        name: string
        sequence: number
        distanceMeters: number
        trackType: string
        location: string
        scoringType: number
        clearScoringType: boolean
        grade: string
        clearGrade: boolean
        classRestriction: string
        clearClassRestriction: boolean
        startsAt: string
        clearStartsAt: boolean
        endsAt: string
        clearEndsAt: boolean
        participantLimit: OptInt
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.AddEventAdmin = this.AddEventAdmin.bind(this)
            this.AddEventMember = this.AddEventMember.bind(this)
            this.AddEventSchedule = this.AddEventSchedule.bind(this)
            this.AddRaceEventMember = this.AddRaceEventMember.bind(this)
            this.AssignRaceResult = this.AssignRaceResult.bind(this)
            this.CreateDataset = this.CreateDataset.bind(this)
            this.CreateEvent = this.CreateEvent.bind(this)
            this.CreateRaceEvent = this.CreateRaceEvent.bind(this)
            this.DeleteEvent = this.DeleteEvent.bind(this)
            this.DeleteRaceEvent = this.DeleteRaceEvent.bind(this)
            this.DeleteRaceResult = this.DeleteRaceResult.bind(this)
            this.GetEvent = this.GetEvent.bind(this)
            this.GetRaceEvent = this.GetRaceEvent.bind(this)
            this.JoinEvent = this.JoinEvent.bind(this)
            this.JoinRaceEvent = this.JoinRaceEvent.bind(this)
            this.LeaveEvent = this.LeaveEvent.bind(this)
            this.LeaveRaceEvent = this.LeaveRaceEvent.bind(this)
            this.ListClassTiers = this.ListClassTiers.bind(this)
            this.ListDatasets = this.ListDatasets.bind(this)
            this.ListEligibleEvents = this.ListEligibleEvents.bind(this)
            this.ListEventAdmins = this.ListEventAdmins.bind(this)
            this.ListEvents = this.ListEvents.bind(this)
            this.ListPublicEvents = this.ListPublicEvents.bind(this)
            this.ListRaceEventMembers = this.ListRaceEventMembers.bind(this)
            this.ListRaceEvents = this.ListRaceEvents.bind(this)
            this.ListRaceResults = this.ListRaceResults.bind(this)
            this.MergeRaceResults = this.MergeRaceResults.bind(this)
            this.RecomputeEventPoints = this.RecomputeEventPoints.bind(this)
            this.RecordLadderMatch = this.RecordLadderMatch.bind(this)
            this.RemoveEventAdmin = this.RemoveEventAdmin.bind(this)
            this.RemoveEventMember = this.RemoveEventMember.bind(this)
            this.RemoveRaceEventMember = this.RemoveRaceEventMember.bind(this)
            this.ReorderRaceEvents = this.ReorderRaceEvents.bind(this)
            this.ReplaceRaceResults = this.ReplaceRaceResults.bind(this)
            this.RestoreEvent = this.RestoreEvent.bind(this)
            this.SetEventPoints = this.SetEventPoints.bind(this)
            this.SetEventSignupsLocked = this.SetEventSignupsLocked.bind(this)
            this.SetEventStatus = this.SetEventStatus.bind(this)
            this.SetUserClass = this.SetUserClass.bind(this)
            this.UpdateDatasetStatus = this.UpdateDatasetStatus.bind(this)
            this.UpdateEvent = this.UpdateEvent.bind(this)
            this.UpdateRaceEvent = this.UpdateRaceEvent.bind(this)
        }

        public async AddEventAdmin(params: AddEventAdminRequest): Promise<AddEventAdminResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                eventId: params.eventId,
                userId:  params.userId,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/event-admins`, JSON.stringify(body), {headers})
            return await resp.json() as AddEventAdminResponse
        }

        public async AddEventMember(params: AddEventMemberRequest): Promise<EventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                eventId: params.eventId,
                userId:  params.userId,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/event-members`, JSON.stringify(body), {headers})
            return await resp.json() as EventDetail
        }

        public async AddEventSchedule(params: AddEventScheduleRequest): Promise<EventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                endsAt:   params.endsAt,
                eventId:  params.eventId,
                location: params.location,
                startsAt: params.startsAt,
                title:    params.title,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/event-schedules`, JSON.stringify(body), {headers})
            return await resp.json() as EventDetail
        }

        public async AddRaceEventMember(params: RaceMemberRequest): Promise<RaceEventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                eventId: params.eventId,
                raceId:  params.raceId,
                userId:  params.userId,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/race-event-members`, JSON.stringify(body), {headers})
            return await resp.json() as RaceEventDetail
        }

        public async AssignRaceResult(params: AssignRaceResultRequest): Promise<RaceResultView> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                clearPosition: params.clearPosition,
                clearStatus:   params.clearStatus,
                eventId:       params.eventId,
                final3F:       params.final3F,
                finishTime:    params.finishTime,
                gateNumber:    params.gateNumber,
                margin:        params.margin,
                passingOrder:  params.passingOrder,
                points:        params.points,
                position:      params.position,
                raceId:        params.raceId,
                resultStatus:  params.resultStatus,
                userId:        params.userId,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/api/race-results`, JSON.stringify(body), {headers})
            return await resp.json() as RaceResultView
        }

        public async CreateDataset(params: CreateDatasetRequest): Promise<DatasetView> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                eventId: params.eventId,
                rows:    params.rows,
                source:  params.source,
                status:  params.status,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/datasets`, JSON.stringify(body), {headers})
            return await resp.json() as DatasetView
        }

        public async CreateEvent(params: CreateEventRequest): Promise<EventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                classRestriction:                params.classRestriction,
                customScoringTables:             params.customScoringTables,
                description:                     params.description,
                granularParticipation:           params.granularParticipation,
                maxConcurrentRaceParticipations: params.maxConcurrentRaceParticipations,
                name:                            params.name,
                organizationId:                  params.organizationId,
                ownerType:                       params.ownerType,
                ownerUserId:                     params.ownerUserId,
                participantLimit:                params.participantLimit,
                scheduledAt:                     params.scheduledAt,
                scoringRulesMode:                params.scoringRulesMode,
                scoringType:                     params.scoringType,
                tag:                             params.tag,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/events`, JSON.stringify(body), {headers})
            return await resp.json() as EventDetail
        }

        public async CreateRaceEvent(params: CreateRaceEventRequest): Promise<RaceEventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                classRestriction: params.classRestriction,
                distanceMeters:   params.distanceMeters,
                endsAt:           params.endsAt,
                eventId:          params.eventId,
                grade:            params.grade,
                location:         params.location,
                name:             params.name,
                participantLimit: params.participantLimit,
                scoringType:      params.scoringType,
                sequence:         params.sequence,
                startsAt:         params.startsAt,
                trackType:        params.trackType,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/race-events`, JSON.stringify(body), {headers})
            return await resp.json() as RaceEventDetail
        }

        public async DeleteEvent(params: DeleteEventRequest): Promise<DeleteEventResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            const query = makeRecord<string, string | string[]>({
                id:        params.ID,
                permanent: String(params.Permanent),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/api/events`, undefined, {headers, query})
            return await resp.json() as DeleteEventResponse
        }

        public async DeleteRaceEvent(params: DeleteRaceEventRequest): Promise<DeleteRaceEventResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            const query = makeRecord<string, string | string[]>({
                "event_id": params.eventId,
                "race_id":  params.raceId,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/api/race-events`, undefined, {headers, query})
            return await resp.json() as DeleteRaceEventResponse
        }

        public async DeleteRaceResult(params: DeleteRaceResultRequest): Promise<DeleteRaceResultResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            const query = makeRecord<string, string | string[]>({
                "event_id": params.eventId,
                "race_id":  params.raceId,
                "user_id":  params.userId,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/api/race-results`, undefined, {headers, query})
            return await resp.json() as DeleteRaceResultResponse
        }

        public async GetEvent(id: string): Promise<EventDetail> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/events/${encodeURIComponent(id)}`)
            return await resp.json() as EventDetail
        }

        public async GetRaceEvent(params: RaceEventQuery): Promise<RaceEventDetail> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                eventId: params.EventID,
                raceId:  params.RaceID,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/race-event`, undefined, {query})
            return await resp.json() as RaceEventDetail
        }

        public async JoinEvent(params: JoinEventRequest): Promise<EventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                eventId: params.eventId,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/event-join`, JSON.stringify(body), {headers})
            return await resp.json() as EventDetail
        }

        public async JoinRaceEvent(params: RaceJoinRequest): Promise<RaceEventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                eventId: params.eventId,
                raceId:  params.raceId,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/race-event-join`, JSON.stringify(body), {headers})
            return await resp.json() as RaceEventDetail
        }

        public async LeaveEvent(params: LeaveEventRequest): Promise<EventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            const query = makeRecord<string, string | string[]>({
                eventId: params.EventID,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/api/event-join`, undefined, {headers, query})
            return await resp.json() as EventDetail
        }

        public async LeaveRaceEvent(params: RaceJoinRequest): Promise<RaceEventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            const query = makeRecord<string, string | string[]>({
                "event_id": params.eventId,
                "race_id":  params.raceId,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/api/race-event-join`, undefined, {headers, query})
            return await resp.json() as RaceEventDetail
        }

        public async ListClassTiers(): Promise<ClassTiersResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/classes`)
            return await resp.json() as ClassTiersResponse
        }

        public async ListDatasets(params: DatasetsQuery): Promise<DatasetsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                eventId: params.EventID,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/datasets-list`, undefined, {query})
            return await resp.json() as DatasetsResponse
        }

        public async ListEligibleEvents(params: EligibleEventsQuery): Promise<EligibleEventsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                userId: params.UserID,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/eligible-events`, undefined, {query})
            return await resp.json() as EligibleEventsResponse
        }

        public async ListEventAdmins(eventID: string): Promise<ListEventAdminsResponse> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/event-admins/${encodeURIComponent(eventID)}`)
            return await resp.json() as ListEventAdminsResponse
        }

        public async ListEvents(params: ListEventsQuery): Promise<ListEventsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                classRestriction: params.ClassRestriction,
                includeDeleted:   String(params.IncludeDeleted),
                limit:            String(params.Limit),
                offset:           String(params.Offset),
                organizationId:   params.OrganizationID,
                status:           params.Status,
                tag:              params.Tag,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/events`, undefined, {query})
            return await resp.json() as ListEventsResponse
        }

        public async ListPublicEvents(params: ListPublicEventsQuery): Promise<ListEventsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:  String(params.Limit),
                offset: String(params.Offset),
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/events-public`, undefined, {query})
            return await resp.json() as ListEventsResponse
        }

        public async ListRaceEventMembers(params: RaceMembersQuery): Promise<RaceMembersResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                eventId: params.EventID,
                raceId:  params.RaceID,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/race-event-members-list`, undefined, {query})
            return await resp.json() as RaceMembersResponse
        }

        public async ListRaceEvents(params: ListRaceEventsQuery): Promise<ReorderRaceEventsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                eventId: params.EventID,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/race-events-list`, undefined, {query})
            return await resp.json() as ReorderRaceEventsResponse
        }

        public async ListRaceResults(params: RaceResultsQuery): Promise<RaceResultsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                eventId: params.EventID,
                raceId:  params.RaceID,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/race-results-list`, undefined, {query})
            return await resp.json() as RaceResultsResponse
        }

        public async MergeRaceResults(params: BulkResultsRequest): Promise<RaceResultsResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                eventId: params.eventId,
                raceId:  params.raceId,
                results: params.results,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/race-results-bulk`, JSON.stringify(body), {headers})
            return await resp.json() as RaceResultsResponse
        }

        public async RecomputeEventPoints(params: RecomputePointsRequest): Promise<RecomputePointsResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                id: params.id,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/event-recompute-points`, JSON.stringify(body), {headers})
            return await resp.json() as RecomputePointsResponse
        }

        public async RecordLadderMatch(params: LadderMatchRequest): Promise<EventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                id:       params.id,
                loserId:  params.loserId,
                winnerId: params.winnerId,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/event-ladder-matches`, JSON.stringify(body), {headers})
            return await resp.json() as EventDetail
        }

        public async RemoveEventAdmin(params: RemoveEventAdminRequest): Promise<AddEventAdminResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            const query = makeRecord<string, string | string[]>({
                eventId: params.EventID,
                userId:  params.UserID,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/api/event-admins`, undefined, {headers, query})
            return await resp.json() as AddEventAdminResponse
        }

        public async RemoveEventMember(params: RemoveEventMemberRequest): Promise<EventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            const query = makeRecord<string, string | string[]>({
                eventId: params.EventID,
                userId:  params.UserID,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/api/event-members`, undefined, {headers, query})
            return await resp.json() as EventDetail
        }

        public async RemoveRaceEventMember(params: RaceMemberRequest): Promise<RaceEventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            const query = makeRecord<string, string | string[]>({
                "event_id": params.eventId,
                "race_id":  params.raceId,
                "user_id":  params.userId,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/api/race-event-members`, undefined, {headers, query})
            return await resp.json() as RaceEventDetail
        }

        public async ReorderRaceEvents(params: ReorderRaceEventsRequest): Promise<ReorderRaceEventsResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                eventId:        params.eventId,
                orderedRaceIds: params.orderedRaceIds,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/api/race-events-reorder`, JSON.stringify(body), {headers})
            return await resp.json() as ReorderRaceEventsResponse
        }

        public async ReplaceRaceResults(params: BulkResultsRequest): Promise<RaceResultsResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                eventId: params.eventId,
                raceId:  params.raceId,
                results: params.results,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/api/race-results-bulk`, JSON.stringify(body), {headers})
            return await resp.json() as RaceResultsResponse
        }

        public async RestoreEvent(params: RestoreEventRequest): Promise<EventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                id: params.id,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/event-restore`, JSON.stringify(body), {headers})
            return await resp.json() as EventDetail
        }

        public async SetEventPoints(params: SetEventPointsRequest): Promise<EventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                id:     params.id,
                points: params.points,
                userId: params.userId,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/api/event-points`, JSON.stringify(body), {headers})
            return await resp.json() as EventDetail
        }

        public async SetEventSignupsLocked(params: SetEventSignupsLockedRequest): Promise<EventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                eventId: params.eventId,
                locked:  params.locked,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/api/event-signups-lock`, JSON.stringify(body), {headers})
            return await resp.json() as EventDetail
        }

        public async SetEventStatus(params: SetEventStatusRequest): Promise<EventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                id:     params.id,
                status: params.status,
                tag:    params.tag,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/api/event-status`, JSON.stringify(body), {headers})
            return await resp.json() as EventDetail
        }

        public async SetUserClass(params: SetUserClassRequest): Promise<SetUserClassResponse> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                classTier:      params.classTier,
                organizationId: params.organizationId,
                userId:         params.userId,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/api/user-class`, JSON.stringify(body), {headers})
            return await resp.json() as SetUserClassResponse
        }

        public async UpdateDatasetStatus(params: UpdateDatasetStatusRequest): Promise<DatasetView> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                datasetId: params.datasetId,
                eventId:   params.eventId,
                status:    params.status,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PUT", `/api/dataset-status`, JSON.stringify(body), {headers})
            return await resp.json() as DatasetView
        }

        public async UpdateEvent(params: UpdateEventRequest): Promise<EventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                classRestriction:                params.classRestriction,
                customScoringTables:             params.customScoringTables,
                description:                     params.description,
                id:                              params.id,
                maxConcurrentRaceParticipations: params.maxConcurrentRaceParticipations,
                name:                            params.name,
                participantLimit:                params.participantLimit,
                scheduledAt:                     params.scheduledAt,
                scoringRulesMode:                params.scoringRulesMode,
                tag:                             params.tag,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/api/events`, JSON.stringify(body), {headers})
            return await resp.json() as EventDetail
        }

        public async UpdateRaceEvent(params: UpdateRaceEventRequest): Promise<RaceEventDetail> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                classRestriction:      params.classRestriction,
                clearClassRestriction: params.clearClassRestriction,
                clearEndsAt:           params.clearEndsAt,
                clearGrade:            params.clearGrade,
                clearScoringType:      params.clearScoringType,
                clearStartsAt:         params.clearStartsAt,
                distanceMeters:        params.distanceMeters,
                endsAt:                params.endsAt,
                eventId:               params.eventId,
                grade:                 params.grade,
                location:              params.location,
                name:                  params.name,
                participantLimit:      params.participantLimit,
                raceId:                params.raceId,
                scoringType:           params.scoringType,
                sequence:              params.sequence,
                startsAt:              params.startsAt,
                trackType:             params.trackType,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/api/race-events`, JSON.stringify(body), {headers})
            return await resp.json() as RaceEventDetail
        }
    }
}

/**
 * Package teammanager implements team (organization) management: teams,
 * membership, and aggregate team statistics.
 * 
 * Mirrors ts-legacy/teammanager/teams.ts, team-members.ts, team-stats.ts,
 * and team-guards.ts. A team is modelled as a better-auth organization row;
 * all state lives in the shared lightwing database (db).
 */
export namespace teammanager {
    /**
     * AddTeamMemberRequest carries the team id, auth header, and new membership.
     */
    export interface AddTeamMemberRequest {
        id: string
        Authorization: string
        userId: string
        role: string
    }

    /**
     * CreateTeamRequest carries the auth header plus the new team's fields.
     */
    export interface CreateTeamRequest {
        Authorization: string
        name: string
        logo: string
    }

    /**
     * GetTeamBySlugParams carries the slug query param. It lives at
     * /api/teams-by-slug (rather than nested under /api/teams/) because static
     * sub-paths route-conflict with /api/teams/:id under Encore's router.
     */
    export interface GetTeamBySlugParams {
        Slug: string
    }

    /**
     * ListTeamMembersRequest carries the team id plus search/pagination query params.
     */
    export interface ListTeamMembersRequest {
        ID: string
        Search: string
        Limit: number
        Offset: number
    }

    export interface ListTeamMembersResponse {
        members: MemberListItem[]
        total: number
    }

    /**
     * ListTeamsRequest carries search/pagination query params.
     */
    export interface ListTeamsRequest {
        Search: string
        Limit: number
        Offset: number
    }

    export interface ListTeamsResponse {
        teams: TeamListItem[]
        total: number
    }

    /**
     * MemberListItem is a membership row for member list views.
     */
    export interface MemberListItem {
        userId: string
        name: string
        slug: string
        role: string
    }

    /**
     * RemoveTeamMemberRequest carries the team/user ids plus the auth header.
     */
    export interface RemoveTeamMemberRequest {
        ID: string
        UserID: string
        Authorization: string
    }

    /**
     * Team is a team with its members and aggregate statistics.
     */
    export interface Team {
        id: string
        name: string
        slug: string
        logo: string
        stats: TeamStats
        administratorSlotsRemaining: number
        members: TeamMemberSummary[]
    }

    /**
     * TeamListItem is a team row for list views.
     */
    export interface TeamListItem {
        id: string
        name: string
        slug: string
        logo: string
        administratorSlotsRemaining: number
        memberCount: number
    }

    /**
     * TeamMemberSummary is a single membership with display name.
     */
    export interface TeamMemberSummary {
        userId: string
        name: string
        role: string
    }

    /**
     * TeamStats holds a team's aggregate "container" statistics.
     */
    export interface TeamStats {
        rankingAverage: number
        pointsAverage: number
        seasonRank: number
        averagePointsPerEvent: number
    }

    /**
     * UpdateTeamMemberRoleRequest carries the team/user ids, auth header, and role.
     */
    export interface UpdateTeamMemberRoleRequest {
        id: string
        userId: string
        Authorization: string
        role: string
    }

    /**
     * UpdateTeamRequest carries the target id, auth header, and editable fields.
     * The id travels in the body because this Encore version only accepts scalar
     * params alongside path params.
     */
    export interface UpdateTeamRequest {
        id: string
        Authorization: string
        name: string
        slug: string
        logo: string
        clearLogo: boolean
    }

    /**
     * UpdateTeamStatsRequest carries the team id, auth header, and stat values.
     */
    export interface UpdateTeamStatsRequest {
        id: string
        Authorization: string
        rankingAverage: number
        pointsAverage: number
        seasonRank: number
        averagePointsPerEvent: number
    }

    export class ServiceClient {
        private baseClient: BaseClient

        constructor(baseClient: BaseClient) {
            this.baseClient = baseClient
            this.AddTeamMember = this.AddTeamMember.bind(this)
            this.CreateTeam = this.CreateTeam.bind(this)
            this.GetTeam = this.GetTeam.bind(this)
            this.GetTeamBySlug = this.GetTeamBySlug.bind(this)
            this.ListTeamMembers = this.ListTeamMembers.bind(this)
            this.ListTeams = this.ListTeams.bind(this)
            this.RemoveTeamMember = this.RemoveTeamMember.bind(this)
            this.UpdateTeam = this.UpdateTeam.bind(this)
            this.UpdateTeamMemberRole = this.UpdateTeamMemberRole.bind(this)
            this.UpdateTeamStats = this.UpdateTeamStats.bind(this)
        }

        public async AddTeamMember(params: AddTeamMemberRequest): Promise<Team> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                id:     params.id,
                role:   params.role,
                userId: params.userId,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/team-members`, JSON.stringify(body), {headers})
            return await resp.json() as Team
        }

        public async CreateTeam(params: CreateTeamRequest): Promise<Team> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                logo: params.logo,
                name: params.name,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("POST", `/api/teams`, JSON.stringify(body), {headers})
            return await resp.json() as Team
        }

        public async GetTeam(id: string): Promise<Team> {
            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/teams/${encodeURIComponent(id)}`)
            return await resp.json() as Team
        }

        public async GetTeamBySlug(params: GetTeamBySlugParams): Promise<Team> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                slug: params.Slug,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/teams-by-slug`, undefined, {query})
            return await resp.json() as Team
        }

        public async ListTeamMembers(params: ListTeamMembersRequest): Promise<ListTeamMembersResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                id:     params.ID,
                limit:  String(params.Limit),
                offset: String(params.Offset),
                search: params.Search,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/team-members`, undefined, {query})
            return await resp.json() as ListTeamMembersResponse
        }

        public async ListTeams(params: ListTeamsRequest): Promise<ListTeamsResponse> {
            // Convert our params into the objects we need for the request
            const query = makeRecord<string, string | string[]>({
                limit:  String(params.Limit),
                offset: String(params.Offset),
                search: params.Search,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("GET", `/api/teams`, undefined, {query})
            return await resp.json() as ListTeamsResponse
        }

        public async RemoveTeamMember(params: RemoveTeamMemberRequest): Promise<Team> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            const query = makeRecord<string, string | string[]>({
                id:     params.ID,
                userId: params.UserID,
            })

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("DELETE", `/api/team-members`, undefined, {headers, query})
            return await resp.json() as Team
        }

        public async UpdateTeam(params: UpdateTeamRequest): Promise<Team> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                clearLogo: params.clearLogo,
                id:        params.id,
                logo:      params.logo,
                name:      params.name,
                slug:      params.slug,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/api/teams`, JSON.stringify(body), {headers})
            return await resp.json() as Team
        }

        public async UpdateTeamMemberRole(params: UpdateTeamMemberRoleRequest): Promise<Team> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                id:     params.id,
                role:   params.role,
                userId: params.userId,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/api/team-members`, JSON.stringify(body), {headers})
            return await resp.json() as Team
        }

        public async UpdateTeamStats(params: UpdateTeamStatsRequest): Promise<Team> {
            // Convert our params into the objects we need for the request
            const headers = makeRecord<string, string>({
                authorization: params.Authorization,
            })

            // Construct the body with only the fields which we want encoded within the body (excluding query string or header fields)
            const body: Record<string, any> = {
                averagePointsPerEvent: params.averagePointsPerEvent,
                id:                    params.id,
                pointsAverage:         params.pointsAverage,
                rankingAverage:        params.rankingAverage,
                seasonRank:            params.seasonRank,
            }

            // Now make the actual call to the API
            const resp = await this.baseClient.callTypedAPI("PATCH", `/api/team-stats`, JSON.stringify(body), {headers})
            return await resp.json() as Team
        }
    }
}

// JSONValue represents an arbitrary JSON value.
export type JSONValue = string | number | boolean | null | JSONValue[] | {[key: string]: JSONValue}


function encodeQuery(parts: Record<string, string | string[]>): string {
    const pairs: string[] = []
    for (const key in parts) {
        const val = (Array.isArray(parts[key]) ?  parts[key] : [parts[key]]) as string[]
        for (const v of val) {
            pairs.push(`${key}=${encodeURIComponent(v)}`)
        }
    }
    return pairs.join("&")
}

// makeRecord takes a record and strips any undefined values from it,
// and returns the same record with a narrower type.
// @ts-ignore - TS ignore because makeRecord is not always used
function makeRecord<K extends string | number | symbol, V>(record: Record<K, V | undefined>): Record<K, V> {
    for (const key in record) {
        if (record[key] === undefined) {
            delete record[key]
        }
    }
    return record as Record<K, V>
}

function encodeWebSocketHeaders(headers: Record<string, string>) {
    // url safe, no pad
    const base64encoded = btoa(JSON.stringify(headers))
      .replaceAll("=", "")
      .replaceAll("+", "-")
      .replaceAll("/", "_");
    return "encore.dev.headers." + base64encoded;
}

class WebSocketConnection {
    public ws: WebSocket;

    private hasUpdateHandlers: (() => void)[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        let protocols = ["encore-ws"];
        if (headers) {
            protocols.push(encodeWebSocketHeaders(headers))
        }

        this.ws = new WebSocket(url, protocols)

        this.on("error", () => {
            this.resolveHasUpdateHandlers();
        });

        this.on("close", () => {
            this.resolveHasUpdateHandlers();
        });
    }

    resolveHasUpdateHandlers() {
        const handlers = this.hasUpdateHandlers;
        this.hasUpdateHandlers = [];

        for (const handler of handlers) {
            handler()
        }
    }

    async hasUpdate() {
        // await until a new message have been received, or the socket is closed
        await new Promise((resolve) => {
            this.hasUpdateHandlers.push(() => resolve(null))
        });
    }

    on(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.addEventListener(type, handler);
    }

    off(type: "error" | "close" | "message" | "open", handler: (event: any) => void) {
        this.ws.removeEventListener(type, handler);
    }

    close() {
        this.ws.close();
    }
}

export class StreamInOut<Request, Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamIn<Response> {
    public socket: WebSocketConnection;
    private buffer: Response[] = [];

    constructor(url: string, headers?: Record<string, string>) {
        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            this.buffer.push(JSON.parse(event.data));
            this.socket.resolveHasUpdateHandlers();
        });
    }

    close() {
        this.socket.close();
    }

    async next(): Promise<Response | undefined> {
        for await (const next of this) return next;
        return undefined;
    }

    async *[Symbol.asyncIterator](): AsyncGenerator<Response, undefined, void> {
        while (true) {
            if (this.buffer.length > 0) {
                yield this.buffer.shift() as Response;
            } else {
                if (this.socket.ws.readyState === WebSocket.CLOSED) return;
                await this.socket.hasUpdate();
            }
        }
    }
}

export class StreamOut<Request, Response> {
    public socket: WebSocketConnection;
    private responseValue: Promise<Response>;

    constructor(url: string, headers?: Record<string, string>) {
        let responseResolver: (_: any) => void;
        this.responseValue = new Promise((resolve) => responseResolver = resolve);

        this.socket = new WebSocketConnection(url, headers);
        this.socket.on("message", (event: any) => {
            responseResolver(JSON.parse(event.data))
        });
    }

    async response(): Promise<Response> {
        return this.responseValue;
    }

    close() {
        this.socket.close();
    }

    async send(msg: Request) {
        if (this.socket.ws.readyState === WebSocket.CONNECTING) {
            // await that the socket is opened
            await new Promise((resolve) => {
                this.socket.ws.addEventListener("open", resolve, { once: true });
            });
        }

        return this.socket.ws.send(JSON.stringify(msg));
    }
}
// CallParameters is the type of the parameters to a method call, but require headers to be a Record type
type CallParameters = Omit<RequestInit, "method" | "body" | "headers"> & {
    /** Headers to be sent with the request */
    headers?: Record<string, string>

    /** Query parameters to be sent with the request */
    query?: Record<string, string | string[]>
}


// A fetcher is the prototype for the inbuilt Fetch function
export type Fetcher = typeof fetch;

const boundFetch = fetch.bind(this);

class BaseClient {
    readonly baseURL: string
    readonly fetcher: Fetcher
    readonly headers: Record<string, string>
    readonly requestInit: Omit<RequestInit, "headers"> & { headers?: Record<string, string> }

    constructor(baseURL: string, options: ClientOptions) {
        this.baseURL = baseURL
        this.headers = {}

        // Add User-Agent header if the script is running in the server
        // because browsers do not allow setting User-Agent headers to requests
        if (!BROWSER) {
            this.headers["User-Agent"] = "poow4-Generated-TS-Client (Encore/v1.58.4)";
        }

        this.requestInit = options.requestInit ?? {};

        // Setup what fetch function we'll be using in the base client
        if (options.fetcher !== undefined) {
            this.fetcher = options.fetcher
        } else {
            this.fetcher = boundFetch
        }
    }

    async getAuthData(): Promise<CallParameters | undefined> {
        return undefined;
    }

    // createStreamInOut sets up a stream to a streaming API endpoint.
    async createStreamInOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamInOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamInOut(this.baseURL + path + queryString, headers);
    }

    // createStreamIn sets up a stream to a streaming API endpoint.
    async createStreamIn<Response>(path: string, params?: CallParameters): Promise<StreamIn<Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamIn(this.baseURL + path + queryString, headers);
    }

    // createStreamOut sets up a stream to a streaming API endpoint.
    async createStreamOut<Request, Response>(path: string, params?: CallParameters): Promise<StreamOut<Request, Response>> {
        let { query, headers } = params ?? {};

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                headers = {...headers, ...authData.headers};
            }
        }

        const queryString = query ? '?' + encodeQuery(query) : ''
        return new StreamOut(this.baseURL + path + queryString, headers);
    }

    // callTypedAPI makes an API call, defaulting content type to "application/json"
    public async callTypedAPI(method: string, path: string, body?: RequestInit["body"], params?: CallParameters): Promise<Response> {
        return this.callAPI(method, path, body, {
            ...params,
            headers: { "Content-Type": "application/json", ...params?.headers }
        });
    }

    // callAPI is used by each generated API method to actually make the request
    public async callAPI(method: string, path: string, body?: RequestInit["body"], params?: CallParameters): Promise<Response> {
        let { query, headers, ...rest } = params ?? {}
        const init = {
            ...this.requestInit,
            ...rest,
            method,
            body: body ?? null,
        }

        // Merge our headers with any predefined headers
        init.headers = {...this.headers, ...init.headers, ...headers}

        // Fetch auth data if there is any
        const authData = await this.getAuthData();

        // If we now have authentication data, add it to the request
        if (authData) {
            if (authData.query) {
                query = {...query, ...authData.query};
            }
            if (authData.headers) {
                init.headers = {...init.headers, ...authData.headers};
            }
        }

        // Make the actual request
        const queryString = query ? '?' + encodeQuery(query) : ''
        const response = await this.fetcher(this.baseURL+path+queryString, init)

        // handle any error responses
        if (!response.ok) {
            // try and get the error message from the response body
            let body: APIErrorResponse = { code: ErrCode.Unknown, message: `request failed: status ${response.status}` }

            // if we can get the structured error we should, otherwise give a best effort
            try {
                const text = await response.text()

                try {
                    const jsonBody = JSON.parse(text)
                    if (isAPIErrorResponse(jsonBody)) {
                        body = jsonBody
                    } else {
                        body.message += ": " + JSON.stringify(jsonBody)
                    }
                } catch {
                    body.message += ": " + text
                }
            } catch (e) {
                // otherwise we just append the text to the error message
                body.message += ": " + String(e)
            }

            throw new APIError(response.status, body)
        }

        return response
    }
}

/**
 * APIErrorDetails represents the response from an Encore API in the case of an error
 */
interface APIErrorResponse {
    code: ErrCode
    message: string
    details?: any
}

function isAPIErrorResponse(err: any): err is APIErrorResponse {
    return (
        err !== undefined && err !== null &&
        isErrCode(err.code) &&
        typeof(err.message) === "string" &&
        (err.details === undefined || err.details === null || typeof(err.details) === "object")
    )
}

function isErrCode(code: any): code is ErrCode {
    return code !== undefined && Object.values(ErrCode).includes(code)
}

/**
 * APIError represents a structured error as returned from an Encore application.
 */
export class APIError extends Error {
    /**
     * The HTTP status code associated with the error.
     */
    public readonly status: number

    /**
     * The Encore error code
     */
    public readonly code: ErrCode

    /**
     * The error details
     */
    public readonly details?: any

    constructor(status: number, response: APIErrorResponse) {
        // extending errors causes issues after you construct them, unless you apply the following fixes
        super(response.message);

        // set error name as constructor name, make it not enumerable to keep native Error behavior
        // https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Operators/new.target#new.target_in_constructors
        Object.defineProperty(this, 'name', {
            value:        'APIError',
            enumerable:   false,
            configurable: true,
        })

        // fix the prototype chain
        if ((Object as any).setPrototypeOf == undefined) {
            (this as any).__proto__ = APIError.prototype
        } else {
            Object.setPrototypeOf(this, APIError.prototype);
        }

        // capture a stack trace
        if ((Error as any).captureStackTrace !== undefined) {
            (Error as any).captureStackTrace(this, this.constructor);
        }

        this.status = status
        this.code = response.code
        this.details = response.details
    }
}

/**
 * Typeguard allowing use of an APIError's fields'
 */
export function isAPIError(err: any): err is APIError {
    return err instanceof APIError;
}

export enum ErrCode {
    /**
     * OK indicates the operation was successful.
     */
    OK = "ok",

    /**
     * Canceled indicates the operation was canceled (typically by the caller).
     *
     * Encore will generate this error code when cancellation is requested.
     */
    Canceled = "canceled",

    /**
     * Unknown error. An example of where this error may be returned is
     * if a Status value received from another address space belongs to
     * an error-space that is not known in this address space. Also
     * errors raised by APIs that do not return enough error information
     * may be converted to this error.
     *
     * Encore will generate this error code in the above two mentioned cases.
     */
    Unknown = "unknown",

    /**
     * InvalidArgument indicates client specified an invalid argument.
     * Note that this differs from FailedPrecondition. It indicates arguments
     * that are problematic regardless of the state of the system
     * (e.g., a malformed file name).
     *
     * This error code will not be generated by the gRPC framework.
     */
    InvalidArgument = "invalid_argument",

    /**
     * DeadlineExceeded means operation expired before completion.
     * For operations that change the state of the system, this error may be
     * returned even if the operation has completed successfully. For
     * example, a successful response from a server could have been delayed
     * long enough for the deadline to expire.
     *
     * The gRPC framework will generate this error code when the deadline is
     * exceeded.
     */
    DeadlineExceeded = "deadline_exceeded",

    /**
     * NotFound means some requested entity (e.g., file or directory) was
     * not found.
     *
     * This error code will not be generated by the gRPC framework.
     */
    NotFound = "not_found",

    /**
     * AlreadyExists means an attempt to create an entity failed because one
     * already exists.
     *
     * This error code will not be generated by the gRPC framework.
     */
    AlreadyExists = "already_exists",

    /**
     * PermissionDenied indicates the caller does not have permission to
     * execute the specified operation. It must not be used for rejections
     * caused by exhausting some resource (use ResourceExhausted
     * instead for those errors). It must not be
     * used if the caller cannot be identified (use Unauthenticated
     * instead for those errors).
     *
     * This error code will not be generated by the gRPC core framework,
     * but expect authentication middleware to use it.
     */
    PermissionDenied = "permission_denied",

    /**
     * ResourceExhausted indicates some resource has been exhausted, perhaps
     * a per-user quota, or perhaps the entire file system is out of space.
     *
     * This error code will be generated by the gRPC framework in
     * out-of-memory and server overload situations, or when a message is
     * larger than the configured maximum size.
     */
    ResourceExhausted = "resource_exhausted",

    /**
     * FailedPrecondition indicates operation was rejected because the
     * system is not in a state required for the operation's execution.
     * For example, directory to be deleted may be non-empty, an rmdir
     * operation is applied to a non-directory, etc.
     *
     * A litmus test that may help a service implementor in deciding
     * between FailedPrecondition, Aborted, and Unavailable:
     *  (a) Use Unavailable if the client can retry just the failing call.
     *  (b) Use Aborted if the client should retry at a higher-level
     *      (e.g., restarting a read-modify-write sequence).
     *  (c) Use FailedPrecondition if the client should not retry until
     *      the system state has been explicitly fixed. E.g., if an "rmdir"
     *      fails because the directory is non-empty, FailedPrecondition
     *      should be returned since the client should not retry unless
     *      they have first fixed up the directory by deleting files from it.
     *  (d) Use FailedPrecondition if the client performs conditional
     *      REST Get/Update/Delete on a resource and the resource on the
     *      server does not match the condition. E.g., conflicting
     *      read-modify-write on the same resource.
     *
     * This error code will not be generated by the gRPC framework.
     */
    FailedPrecondition = "failed_precondition",

    /**
     * Aborted indicates the operation was aborted, typically due to a
     * concurrency issue like sequencer check failures, transaction aborts,
     * etc.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     */
    Aborted = "aborted",

    /**
     * OutOfRange means operation was attempted past the valid range.
     * E.g., seeking or reading past end of file.
     *
     * Unlike InvalidArgument, this error indicates a problem that may
     * be fixed if the system state changes. For example, a 32-bit file
     * system will generate InvalidArgument if asked to read at an
     * offset that is not in the range [0,2^32-1], but it will generate
     * OutOfRange if asked to read from an offset past the current
     * file size.
     *
     * There is a fair bit of overlap between FailedPrecondition and
     * OutOfRange. We recommend using OutOfRange (the more specific
     * error) when it applies so that callers who are iterating through
     * a space can easily look for an OutOfRange error to detect when
     * they are done.
     *
     * This error code will not be generated by the gRPC framework.
     */
    OutOfRange = "out_of_range",

    /**
     * Unimplemented indicates operation is not implemented or not
     * supported/enabled in this service.
     *
     * This error code will be generated by the gRPC framework. Most
     * commonly, you will see this error code when a method implementation
     * is missing on the server. It can also be generated for unknown
     * compression algorithms or a disagreement as to whether an RPC should
     * be streaming.
     */
    Unimplemented = "unimplemented",

    /**
     * Internal errors. Means some invariants expected by underlying
     * system has been broken. If you see one of these errors,
     * something is very broken.
     *
     * This error code will be generated by the gRPC framework in several
     * internal error conditions.
     */
    Internal = "internal",

    /**
     * Unavailable indicates the service is currently unavailable.
     * This is a most likely a transient condition and may be corrected
     * by retrying with a backoff. Note that it is not always safe to retry
     * non-idempotent operations.
     *
     * See litmus test above for deciding between FailedPrecondition,
     * Aborted, and Unavailable.
     *
     * This error code will be generated by the gRPC framework during
     * abrupt shutdown of a server process or network connection.
     */
    Unavailable = "unavailable",

    /**
     * DataLoss indicates unrecoverable data loss or corruption.
     *
     * This error code will not be generated by the gRPC framework.
     */
    DataLoss = "data_loss",

    /**
     * Unauthenticated indicates the request does not have valid
     * authentication credentials for the operation.
     *
     * The gRPC framework will generate this error code when the
     * authentication metadata is invalid or a Credentials callback fails,
     * but also expect authentication middleware to generate it.
     */
    Unauthenticated = "unauthenticated",
}
